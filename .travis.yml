language: node_js

services:
  - couchdb

sudo: false
node_js:
  - "stable"
script: npm run $COMMAND

env:
  matrix:
  - COMMAND=test
  - CLIENT=selenium:firefox COMMAND=test
  - CLIENT=selenium:phantomjs COMMAND=test
  - COMMAND=coverage

branches:
  only:
  - master
